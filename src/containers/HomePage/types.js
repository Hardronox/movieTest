import PropTypes from 'prop-types';

// TODO: add propTypes for the page
export const propTypes = {
  maxDisabledDay: PropTypes.object,
  isCartCountLoading: PropTypes.bool,
  isCartCountSuccess: PropTypes.bool,
};

// Specifies the default values for props:
export const defaultProps = {
  //title: 'SPECS NEEDED',
};
